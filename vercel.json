{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo 'No dependencies needed at deploy time.'",
  "buildCommand": "echo 'No build at deploy time: this is a static site and the bundle is committed at assets/app.min.js (rebuild locally with npm run build).'",
  "outputDirectory": "."
}
